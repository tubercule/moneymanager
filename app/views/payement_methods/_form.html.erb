<%= form_for(@payement_method) do |f| %>
  <% if @payement_method.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@payement_method.errors.count, "error") %> prohibited this payement_method from being saved:</h2>

      <ul>
      <% @payement_method.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
